
# === FILE 1: package.json ===
package_json = '''{
  "name": "webcam-recorder",
  "version": "1.0.0",
  "description": "Sito di registrazione webcam con pannello admin",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
'''

with open(f"{base_dir}/package.json", "w") as f:
    f.write(package_json)

print("✅ Creato: package.json")
